= content_tag_for :div, timer, class: "row" do
  .col.s3
    = timer.start.to_s( :db )
  .col.s3
    = timer.finish.to_s( :db )
  .col.s3
    = delta_time timer.start, timer.finish
  .col.s3
    = link_to task_timer_path(@task.id, timer.id), remote: true, method: :delete, class: "destroy-timer" do
      %i.material-icons delete
